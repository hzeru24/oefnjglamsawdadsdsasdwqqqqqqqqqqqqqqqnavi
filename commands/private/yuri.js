const { MessageEmbed, MessageActionRow, MessageButton } = require('discord.js')

module.exports = {
    name: 'teeest',
    category: 'private',
    run: async (client, message, args,) => {
      
    },
    }